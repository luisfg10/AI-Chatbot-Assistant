# ------------------------------------------------------------------
# Memory Compacting

memory compacting:
  template: |
    Long-term memory:
    {long term memory}
    Recent interactions:
    {short term memory}
